name: Deploy

on:
  push:
    branches: [main]

jobs:
  deploy:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
      - name: Railway deploy placeholder
        run: |
          echo "Add Railway deploy action here after connecting Railway project"
      - name: Vercel deploy placeholder
        run: |
          echo "Add Vercel deploy action here after connecting Vercel project"
