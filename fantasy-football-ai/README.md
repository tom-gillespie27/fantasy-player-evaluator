# Fantasy Football AI Draft Evaluator

This repository contains the scaffold for a Fantasy Football AI Draft Evaluator project.

## Structure

- `backend/` — backend API, services, ML pipelines, and tests
- `frontend/` — frontend placeholder (scaffolded separately)
- `.github/workflows/` — CI and deployment workflows
- `docker-compose.yml` — local dev container orchestration

## Next steps

1. Implement backend logic in `backend/app/`
2. Add frontend scaffold in `frontend/`
3. Configure CI and deployment workflows
